def number_length(num):
	#Criar o contador
	count = 0

	#Transformar o num em uma variavel string
	string = str(num)

	#Agora é possivel o loop percorrer o num que esta alocado no string
	for i in string
		count += 1

	#Retornar o contador com a qnt de numeros obtidos no loop
	return count
